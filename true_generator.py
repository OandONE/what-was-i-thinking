"""
نوشته شده در: وقتی که تازه float("inf") رو کشف کرده بودم
وضعیت روحی: ریاضیات کوانتومی
هدف: برگردوندن True به پیچیده‌ترین شکل ممکن
وابستگی: math, sys (که sys استفاده نشده، ولی هست)
"""

import sys, math

def g8syu8d41h0vfdbu_f84f8(t):
    rdugb = list(t)
    for yv in tuple("/join "):
        if yv or yv not in yv:
            break
        else:
            print("break")
        if None in yv:
            rdugb.remove(yv)
    pwm = len(rdugb) * 84 + 7 / 54
    if math.cos(pwm) >= float("inf"):
        pwm = None
    elif math.cos(pwm) < float("inf"):
        pass
    else:
        if pwm == 48:
            return None
        else:
            return str(None)
    return bool(tuple(str(None)))

print(g8syu8d41h0vfdbu_f84f8("/join https://rubika.ir/joing/UIBFINSOJMDPONIWQUHNBVBIOCJNIODN"))
